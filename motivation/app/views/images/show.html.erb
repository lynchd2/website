	<div class="container-image">
		<%= image_tag(@image.url, class: "image-display") %>
	
	<aside class="image-motivate-information-container">
			<% if @user %>
				<%= form_for([@user, @favorite]) do |f| %>
					<%= f.hidden_field :image_id, value: @image.id %>
					<%= f.hidden_field :url, value: @image.url %>
					<%= f.hidden_field :user_id, value: @user.id %>
					<%= f.submit "Make Favorite", class: "general-button favorite" %>
				<% end %>
				<%= form_for([@user, @unmotivational_image]) do |f| %>
					<%= f.hidden_field :image_id, value: @image.id %>
					<%= f.hidden_field :user_id, value: @user.id %>
					<%= f.hidden_field :url, value: @image.url %>
				<%= f.submit "Unmotivational", class: "general-button" %>
				<% end %>
			<% else %>
				<%= button_to "Make Favorite", new_user_path, method: :get, class: "general-button favorite", params: {favorite_sign_in: true} %>
				<%= button_to "Unmotivational", new_user_path, method: :get, class: "general-button unmotivational", params: {unmotivational_sign_in: true} %>
			<% end %>
	</aside>
	</div>
<div class= "container-image-show-buttons">
		<%= button_to "Back to the image categories", image_categories_path, method: :get, class: "general-button before-navigation left" %>
		<%= button_to "Give me more motivation" , image_path, method: :get, class: "general-button before-navigation right", params: {type: @type} %>
</div>